@using UniCampaignE.Web.Localization
@{
    Layout = "~/Views/Shared/_UniCampaignLayout.cshtml";
}
@section page_content{
    <div class="content-heading">
      Dealer Agent
        <div class="pull-right">
            
            @*<div class="btn-group">
                    <a class="btn btn-primary bg-primary-dark text-bold" data-ajax="true" data-ajax-loading="#loader" data-ajax-method="GET" data-ajax-mode="replace" data-ajax-success="showModalEditor" data-ajax-failure="globalAjaxErrorHandler" data-ajax-update="#modal_editor" href="@Url.RouteUrl(new {area="",controller="ContactList",action="AddandAssignMain" })">
                        <span>DirectAssign</span>&nbsp;&nbsp;<i class="fa fa-plus"></i>
                    </a>
                </div>*@
        </div>
    </div>
    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-flat panel-primary">
                <div class="panel-heading">
                    <div class="panel-title">
                      Dealers Agents
                    </div>
                </div>
                <div class="panel-body">
                    <div class="table-responsive p0">
                        <table id="grid" class="table table-striped table-bordered table-condensed" data-source="@Url.Action(nameof(UniCampaignE.Web.Controllers.DealerAgentController.ListAgents))"></table>
                    </div>
                </div>
            </div>
        </div>
    </div>
}
@section page_scripts{
    <script type="text/javascript">
        var selectedList = null;
        $(document).ready(function (e) {
            grid = $("#grid").grid({
                primaryKey: "",
                uiLibrary: "bootstrap",
                iconsLibrary: 'fontawesome',
                notFoundText: "@GridComman.GRID_NO_RECORD_FOUND",
                columns: [
                    { field: "AgentID", title: "AgentID" },
                    { field: "SkillTargetID", title: "SkillTargetID" },
                    { field: "EnterpriseName", title: "EnterpriseName" },
                      { field: "LoginName", title: "LoginName" },
                        { field: "TemporaryAgent", title: "TemporaryAgent" },
                        { field: "PersonID", title: "PersonID" },
                   // { field: 'Peripheral', title: 'Peripheral' },
                    {field:'IsSupervisor',title:'Supervisor'}
                ],
                pager: { limit: 8, sizes: [8, 15, 25] }
            });
            function editList(e) {
                selectedList = e.data.record;
            }
        });
    </script>
}
